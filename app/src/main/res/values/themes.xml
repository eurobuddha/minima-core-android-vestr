<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Always-dark theme so Material widgets (spinners, buttons, inputs) render dark regardless of
         the system light/dark setting, matching the app's dark + orange palette. -->
    <style name="Base.Theme.Utxo" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">#F7931A</item>
        <item name="colorAccent">#F7931A</item>
        <item name="android:colorBackground">#0A0A0F</item>
    </style>

    <style name="Theme.Utxo" parent="Base.Theme.Utxo" />
</resources>
